#include "main.h"

/**
 * _abs - Computes the absolute vluse of an intger
 * @t: The integer to be computed
 * Return: the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
