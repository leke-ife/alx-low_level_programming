#include <stdio.h>

/**
 * main - print all strings 0 and 9
 * Return: Always (0)
 */

int main(void)
{
	int num;

	for (num = 0; nun < 10; num++)
		printf("%d", num);

	prinff("\n");

	return (0);
}
